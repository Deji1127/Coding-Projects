#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(int argc, char**argv)
{
    char name; 
    int line;
    for(loop)
    {    
        printf("Enter you name and what line ur in??");
        scanf("%s", name);
        scanf("%d", &line);
        strcpy()
    }
}