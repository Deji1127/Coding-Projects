#include <stdio.h>
#include "line_header.h"
#include <stdlib.h>
#include <string.h>
typedef struct line
{
    char name;
    int line_order;
    struct line *next;
} LINE;
struct line_one
{
    char name;
    int line_order;
    struct line *next;
};
struct line_two
{
    char name;
    int line_order;
    struct line *next;
};
struct Node* combine(struct Node* line_one, struct Node* line_two);	
{
    for(loop)
    {    
        strcpy(line_one->name, LINE->name);
        strcpy(line_two->name, LINE->name[i]);
        LINE->line_order = line_one->line_order;
        LINE->line_order = line_one->line_order+1
    }
}
struct Node* reverse_list_rec(struct Node* head_ref);	
{
    ????????????
}